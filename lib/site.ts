/**
 * Single source of truth for the site's canonical URL.
 * When a custom domain (e.g. pelumi.dev) goes live, change SITE_URL here only.
 */
export const SITE_URL = "https://pelumi-adewara.vercel.app";

// Short label shown in social cards / UI. Keep in sync conceptually with SITE_URL.
export const SITE_LABEL = "pelumi-adewara.vercel.app";
